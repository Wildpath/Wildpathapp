name: Supabase keep-alive

on:
  schedule:
    - cron: '0 0 */3 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase project
        run: |
          curl -sS -o /dev/null -w "Supabase ping status: %{http_code}\n" https://nkrphmjzyzeplzcgndxg.supabase.co
